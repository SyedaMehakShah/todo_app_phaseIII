/**
 * Frontend authentication client.
 * This connects to the backend API for authentication.
 */

// For frontend, we don't need to initialize a database
// We'll use the API endpoints defined in auth-client.ts
export {};
